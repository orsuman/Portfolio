<% if lawyer.comments.count == 0 %>
  <h2>このユーザーのコメント投稿は０件です</h2>

<% else %>

  <h3>コメント投稿件数 <%= lawyer.comments.count %>件（"<span class="glyphicon glyphicon-thumbs-up"></span>参考になった"が全部で<%= references %>件ついています）</h3></br>

  <table ~~~ class = "table">

    <colgroup>
       <col style = "width:20%;">
       <col style = "width:80%;">
    </colgroup>

    <tr class= "info">
  	<th class = "text-center">カテゴリー</th>
  	<th>タイトル</th>
    </tr>

  <% lawyer.comments.each do |comment| %>
    <tr>
  	<td class = "text-center">
      <%= comment.trouble.category.name %>
  	</td>
  	<td>
      <%= link_to trouble_path(comment.trouble_id) do%>
        <%= comment.comment.truncate(40) %>
      <% end %>
  	</td>
    </tr>
  <%end%>

  </table>

<% end %>